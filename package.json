{
  "name": "otto-display-orchestrator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@otto/command-service": "file:../otto-command-service",
    "@otto/protocol": "file:../otto-protocol"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
